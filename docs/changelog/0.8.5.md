# 0.8.5 — 2026-08-20

Clearer Stage 2 copy (“job types to try”), pin embedding provider for O\*NET match, reuse Prompt Guard client. Frontend is **0.8.5**.

### Frontend

- Step 4: **Job types to try** (wizard hero, panel summary, rationale heading).
- Package version **0.8.5**.

### Backend

- Progress / Swagger prose: job types to try.
- `[embedding].provider` required (`hf-inference` in examples) so aptitude embed uses a `feature-extraction`-capable route.
- Prompt Guard: shared cached `InferenceClient` across chunks.
- Config comment on `malicious_score_threshold`.

### Tests

- RoleFamilyPlanDisplay snapshot copy update.

### Documentation

- **`docs/v0.8.5/job-types-copy-and-embedding-provider.md`** — this release write-up.
- Active docs/READMEs aligned to “job types to try”.
- **`docs/changelog/0.8.5.md`** — this file.

[0.8.5]: https://github.com/mattburnett-repo/aptitude-search/releases/tag/v0.8.5

---
